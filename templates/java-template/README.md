# Java Project Template

Этот шаблон создает Java проект с преднастроенным CI/CD пайплайном.

## Быстрый старт

1. **Создайте новый репозиторий из этого шаблона**
2. **Настройте проект:**
   ```bash
   # Переименуйте Dockerfile
   mv Dockerfile.java Dockerfile
   
   # Обновите .ci-config.yaml
   nano .ci-config.yaml
